version: "2"
run:
  timeout: 5m

linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
  exclusions:
    presets:
      - comments
      - common-false-positives
      - legacy
